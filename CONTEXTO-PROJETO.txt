========================================================================
CONTEXTO DO PROJETO — AGENTES IA (SAC no WhatsApp/e-mail com Hermes)
Handoff pra uma nova sessão de Claude Code. Atualizado 10/07/2026.
========================================================================

# LEIA ISTO PRIMEIRO — ARQUITETURA EM 2 FASES
- FASE ATUAL (este servidor): 3 agentes JUNTOS num VPS só, compartilhando 1 Neon e 1 dashboard.
  Foi o começo/MVP. As specifics abaixo (números, page_id, tokens, wa_router) são DESTE servidor.
- MODELO NOVO (decidido): cada cliente novo fica SOLO no SEU PRÓPRIO servidor (1 container Hermes,
  1 número, e o seu próprio banco/dashboard). NÃO replicar a bagunça multi-tenant deste VPS.
  Use a PARTE B como molde. A PARTE A é referência de como o primeiro ambiente funciona.

O que é o produto: agente de IA que ATENDE no WhatsApp (e e-mail) 24h — qualifica lead, contorna
objeção, AGENDA na Google Agenda. Motor = Hermes (Nous Research) self-hospedado em Docker.
Cockpit = dashboard Next.js no Vercel que MONITORA/CONTROLA (fica FORA do Hermes).

========================================================================
PARTE A — ESTE SERVIDOR (prod atual, MULTI-AGENTE) — específico daqui
========================================================================
3 containers no VPS (/opt/hermes/hermesN_<cliente>/data -> /opt/data, bind rw):
- hermes1_agente24horas — persona NINA. Produto "Agente24Horas". WhatsApp +55 11 99668-1596
  (phone_id 115216611574100). Campanha de FORMULÁRIO Meta, página Amanda Felix 109902140539351.
- hermes3_casaldotrafego — persona AMANDA. Agência de tráfego. WhatsApp +55 66 9209-8891
  (phone_id 414594695067374) + e-mail Brevo (oportunidade@casaldotrafego.email). Recebe prospecção do Minerador.
- hermes2_drlucas — Dr. Lucas (dermato). WhatsApp dele ainda sem credencial Meta. Google Agenda DELE
  conectada via OAuth (10/07).
Os 2 números oficiais estão no MESMO app Meta (917411172673844) -> dividem 1 webhook -> ROTEADOR
wa_router (por phone_id) em hermes.casaldotrafego.com/whatsapp/webhook. (Isso é gambiarra de multi-tenant;
cliente solo NÃO precisa de roteador — webhook vai direto pro container dele.)

Neon central (1 só, compartilhado): schema por agente (conversations+messages) + tabelas public.*
(meta_leads, outreach_convos/msgs, campaigns, outreach_sent, scheduled_dispatches).
Dashboard (1 só, multi-agente): agente-ia.casaldotrafego.com, repo github.com/drtrafego/mvp_agente_ia,
lê os 3 do Neon central. Vercel auto-deploy no push. Envs: DATABASE_URL, DASHBOARD_PASSWORD,
PAINEL_API_TOKEN, DISPATCH_CRON_SECRET.
Orquestração/crons no VPS: /opt/gastaomatos/hermes/ (central-db sync 5min, meta-leads, dispatch-cron 10min,
agente_painel = control API pra pausar/responder, minerador, docs/, CLAUDE.md control-plane).

========================================================================
PARTE B — MOLDE DE CLIENTE NOVO (SOLO, no servidor próprio dele)
========================================================================
Cada cliente = 1 servidor isolado. Passos (referência; há tooling no VPS atual em
/opt/gastaomatos/hermes: skills provisionar-cliente-hermes/criar-agente-hermes + PLAYBOOK-CLIENTE-NOVO-SAC.txt):
1) 1 container Hermes com a imagem hermes-agent-sac:latest (já traz: faster-whisper, trava de travessão,
   echo-off, grace de rajada, patch de sessão de e-mail). Volume /opt/data = HERMES_HOME.
2) WhatsApp: Cloud API OFICIAL do cliente (hermes whatsapp-cloud). Como é solo, o webhook da Meta aponta
   DIRETO pro container (sem wa_router). Portões a abrir: WHATSAPP_CLOUD_ALLOW_ALL_USERS=true,
   HOME_CHANNEL, gateway_restart_notification=false.
3) Persona: montar o SOUL.md (/opt/data/SOUL.md) com o negócio do cliente. Travas obrigatórias:
   anti-invenção (não prometer ligação/retorno humano; não inventar horário sem rodar calendar list),
   não abrir links, sem travessão, só responder com base na base de conhecimento. Testar: hermes chat -q "<msg>".
4) Google Agenda: usar SERVICE ACCOUNT (o cliente compartilha a agenda dele com o email do robô
   @projeto.iam.gserviceaccount.com, permissão "Fazer alterações nos eventos" — igual add usuário no Analytics).
   Lib google.oauth2.service_account já instalada; FALTA adaptar google_api.py p/ autenticar via chave JSON.
   (Dr. Lucas ficou no OAuth por ser o primeiro; novos = service account.)
5) E-mail (opcional): Brevo (MCP send + Inbound Parsing webhook) se o cliente for atender e-mail.
6) Dashboard: decidir 1-por-cliente (deploy próprio lendo o Neon/DB dele) OU multi-tenant. Hoje é 1 só;
   pra cliente solo o certo é o dashboard apontar pro banco DAQUELE cliente.

========================================================================
REGRAS DE OURO (universais, valem sempre)
========================================================================
- NUNCA misturar dados de clientes (cada um isolado: banco/tabela/página/allowlist).
- NUNCA travessão (—) em texto público (trava no código troca por vírgula).
- Deliverables em .txt, salvos na pasta do projeto. Atualizar doc/memória EXISTENTE, nunca duplicar.
- Não apagar histórico/conversas reais; só dados de teste que EU criei.
- SOUL com as travas (anti-invenção, não abrir links). Testar comportamento com hermes chat -q.

========================================================================
DASHBOARD (mvp_agente_ia) — features já prontas (reaproveitáveis)
========================================================================
Visão geral ciente do agente (leadSource form/outreach/none); Conversas (abas WhatsApp/E-mail + filtro
Todas/Ativas24h/Responderam + tag de origem; conversa aberta estilo WhatsApp); Leads (tabela + disparo +
Programar); Mensagens (templates); Campanhas (criar/editar/disparar; vars = ARRAY jsonb via sql.json, NUNCA
JSON.stringify); Disparos (programar/agendar + auto-recuperação toggle OFF, cron + DISPATCH_CRON_SECRET);
Pipeline. Pausar/responder via control API (PAINEL_API_TOKEN).

========================================================================
PENDÊNCIAS ABERTAS (10/07/2026)
========================================================================
- Setar DISPATCH_CRON_SECRET na Vercel (valor em /opt/gastaomatos/hermes/dispatch-cron/.secret) -> liga disparos.
- Testar disparo manual, depois ligar Auto-recuperação (campanha agente24horas:recuperacao-cadastro pronta).
- Persistir envios manuais/disparos no histórico do dashboard (hoje somem ao recarregar).
- Adaptar Google skill (google_api.py) p/ SERVICE ACCOUNT (pré-requisito do modelo cliente-solo).
- Credencial Meta do Dr. Lucas p/ ligar o WhatsApp dele.
- Definir arquitetura do dashboard pro modelo solo (1 por cliente x multi-tenant).
- Rotacionar token Meta system-user p/ escopo só de messaging.

Memórias estruturadas (se copiadas pro ~/.claude do servidor novo): dashboard-agentes-mvp,
disparo-programavel-auto-recuperacao, projeto-hermes-agente24horas, nunca-misturar-clientes.
Este arquivo vive no repo mvp_agente_ia (git clone + leia este .txt = panorama completo).
========================================================================
